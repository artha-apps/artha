# Phase A validation results — headed run

**Build:** packaged unsigned `electron-builder --dir` from `phase-a/provider-foundation` (through the review-remediation + ModelPicker-fix commits) · **Platform:** macOS (Darwin 25.5.0, Apple Silicon, 128 GB) · **Date:** 2026-07-22/23 · **Method:** real packaged app driven over CDP (`--remote-debugging-port`), disposable profiles via `ARTHA_QA_MODE=1` + `ARTHA_USER_DATA_DIR` (founder-approved isolation; the live profile was never opened by any session). Evidence screenshots in `./evidence/` — all captured on disposable profiles with synthetic data only.

| Row | Scenario | Result | Notes / evidence |
|---|---|---|---|
| 1 | Fresh local onboarding | **PASS** | Persona → local path → installed-model pick → chat round-trip on `qwen2.5:14b` (also exercised once end-to-end incl. recommended-model pull + REAL macOS keychain prompt during the founder's own session). ModelPicker chip defect found here → **fixed + regression-tested + re-verified**: chip updates with zero interaction on local and BYOK activation paths |
| 2 | Fresh BYOK onboarding | **PASS** (mock provider) | Preset → key → Find models → test-before-activate → chat; streaming reply rendered; `evidence/row2-byok-chat.png`. Mock provider log shows requests arriving DIRECTLY from the desktop app (no gateway). Real-provider smoke (any live key, ~2 min) left for the founder — no live keys were available to the validation run by design |
| 3 | Configure later | **PASS** | Overlay closes to the honest empty state: banner card (both setup paths named + Open Model Settings action), "No model" chip, and a send-blocking "No model selected → Choose model" toast. NOTE: the composer blocks BEFORE dispatch (better than the matrix's original expectation); the typed `NoModelConfiguredError` remains the backstop for programmatic paths (unit-tested). `evidence/01-no-model-state.png` |
| 4 | Invalid key | **PASS** (real internet) | Bogus key against real `api.openai.com`: normalized auth error, provider's own partial-redaction only, full key never echoed; also proves desktop→provider direct routing. `evidence/row4-invalid-key.png` |
| 5 | Model discovery | **PASS** | GET /v1/models → datalist (2 models, deduped, sorted), first auto-filled |
| 6 | Discovery unavailable → manual fallback | **PASS** | Dead endpoint → "Could not reach the provider…" + manual model entry accepted |
| 7 | Provider activation | **PASS** | Second model added with pre-save Test connection (latency shown), Save & activate |
| 8 | Restart with cloud active | **PASS** | Model persisted; sealed key reopened (no re-entry); zero Ollama lifecycle states; chat works; `strings artha.db` contains no plaintext key. `evidence/02-cloud-active-no-ollama-warning.png` |
| 9 | Provider switching | **PASS** | Panel-activation and picker-switch both directions; chip updates live; chat on switched model |
| 10 | Unavailable local runtime / zero unintended localhost calls | **PASS** (controlled-stub method, founder-approved) | Genuine unavailability produced by a sanitized 127.0.0.1 stub holding :11434 (logs only timestamp/method/path/status; never headers, bodies or credentials; returns a controlled error and does not imitate Ollama; the Ollama binary and config were never modified). **Detection:** runtime reported `{phase:'error', detail:'Ollama did not start in time.'}` — never "ready". **Bounded polling:** 1 spawn attempt, ~600 ms probe interval inside a single 20 s deadline, 73 probes total across the launch + onboarding cycles, and **0 further probes during a 45 s idle** (no runaway loop). **User recovery:** honest onboarding card ("Ollama is installed but isn't responding — something else may be using its port…") with Recheck + a BYOK escape hatch; main-window banner offers Try again; local chat fails visibly. **Cloud unaffected:** cloud session simultaneously chatted fine with **5 loopback requests, all read-only GETs, zero warm/unload/generate/fallback calls**. Restarting Ollama restored the expected state and the release app reconnected. `evidence/row10-runtime-unavailable.png` |
| 11 | No secure keychain | **PASS** (`ARTHA_FORCE_NO_KEYCHAIN=1`) | Persistent save refused with remediation; session-only offered and works; DB stores only the `v1:session` sentinel — zero key material on disk. `evidence/03-secure-storage-unavailable.png` |
| 12 | Session key across restart | **PASS** (chat path) | Typed expiry message with re-enter guidance; no silent failure. Panel badge not visually confirmable with a `127.0.0.1` mock (known review finding L7: local-address endpoints filtered from the cloud list) — badge logic unit-level only. `evidence/04-session-key-expired.png` |
| 13 | Embeddings unavailable — no invalid vectors | **PASS** (after the Phase A integrity patch; previously FAIL) | Indexed a synthetic corpus with the embedder unavailable: **2 chunks persisted, 0 zero-vector rows, both marked `pending_embedding` with `embedding: null`**, chunk text retained for keyword use. `doc_count` reported **0** (embedded chunks only) so a degraded index cannot look healthy. Semantic retrieval excluded them; **0 cloud embedding requests** (verified against the cloud mock's request log). Degraded notice visible before indexing with remediation. **Recovery verified:** with the embedder restored, rebuilding the same index upgraded both chunks to valid 768-dim vectors (`doc_count` 0→2, 0 pending). `evidence/row13-rag-degraded.png` |
| 14 | Existing-user upgrade migration | **PASS** (synthetic DB) | Pre-Phase-A-shaped DB with synthetic plaintext keys + oauth tokens: launch #1 sealed 2 api_keys + 2 oauth tokens, repaired 1 provider id, created the Default profile (byok); launches #2/#3 ran zero re-migrations; `integrity_check` ok; **byte-level scan (incl. WAL) found zero plaintext remnants** after checkpoint+VACUUM |

**Defects found during the headed run — all fixed in-PR with tests:**
1. **ModelPicker stale chip** after onboarding — fixed (event subscription), regression-tested, re-verified headed.
2. **Row 13 blocker — zero-vector persistence** (founder-rejected as "warned ≠ prevented"): the indexer could still write fabricated 768-zero vectors. Fixed with the integrity patch (typed `EmbeddingUnavailableError`, `pending_embedding` state, retrieval exclusion, validated memory-embedding cache); 20 tests.
3. **Runtime status race** found by the row-10 stub: `model:ensure` returned the shared `lastStatus`, which a concurrent launch-time run overwrote, so onboarding showed "Starting your local model…" for a runtime that had already failed. `ensureModelReady` now returns its own terminal status; 5 tests.
4. **QA profile isolation leak** found during the row-13 rerun: `handlers.ts` captured the RAG indexer at module load — before `main.ts` applies `ARTHA_USER_DATA_DIR` — so an isolated session wrote its index file into the real profile directory. Content was synthetic-only and the live DB was unaffected (`getDb()` resolves lazily); the stray file was removed and the live profile verified clean (`integrity_check` ok, no `execution_profiles` table, no `rag-indexes` dir). Indexer is now resolved lazily, with a static regression guard.
5. Not product defects: the first-run feature tour intercepts programmatic input (driver-only), and L7 mock-only badge visibility (pre-recorded review finding, deferred).

**Environment note for BYOK users on macOS:** rebuilding/updating the app changes the binary, so macOS re-prompts for keychain access on the next sealed-key use (this blocked several validation launches until answered). Not a defect, but a real auto-update UX consideration — recorded as a follow-up.

**Honesty limits:** cross-platform native keychain behavior (Linux Secret Service, Windows DPAPI) remains manually unvalidated — CI runs the mocked policy suite on all three OSes, which proves policy logic, not native integration. macOS keychain WAS genuinely exercised (authorization prompt answered on a real Keychain). Byte-level cleanup claims cover the active SQLite file and WAL only — historical filesystem copies, Time Machine snapshots, and previously freed disk blocks are outside what the implementation can guarantee, and no forensic-erasure claim is made.
